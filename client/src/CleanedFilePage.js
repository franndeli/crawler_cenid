import React from 'react';
import { Link } from 'react-router-dom';

function CleanedFilePage() {
    return (
      <div>
        <h1>Archivo Limpio</h1>
        {/* Contenido de la página de archivo limpio, como el enlace de descarga */}
      </div>
    );
  }
  
  export default CleanedFilePage;